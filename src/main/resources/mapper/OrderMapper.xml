<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 🌟 namespace を com.example.ecsample から com.example.demo へ修正 -->
<mapper namespace="com.example.demo.mapper.OrderMapper">

  <insert id="insertOrder" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO orders (user_id)
    VALUES (#{userId})
  </insert>

  <insert id="insertOrderItems">
    INSERT INTO order_items (order_id, product_id, product_price, quantity)
    VALUES
    <foreach collection="list" item="item" separator=",">
      (#{item.orderId}, #{item.productId}, #{item.productPrice}, #{item.quantity})
    </foreach>
  </insert>

  <select id="findHistoryByUserId" resultType="com.example.demo.entity.OrderHistoryRow">
    SELECT
      o.id AS order_id,
      o.created_at,
      p.name AS product_name,              <!-- 🌟 productsテーブル(p)から商品名を取得 -->
      oi.product_price AS price,          <!-- 🌟 カラム名 product_price を Javaの price にマッピング -->
      oi.quantity,
      (oi.product_price * oi.quantity) AS subtotal <!-- 🌟 正しいカラム名で小計を計算 -->
    FROM orders o
    INNER JOIN order_items oi ON o.id = oi.order_id
    INNER JOIN products p ON oi.product_id = p.id  <!-- 🌟 ここで商品マスタテーブルを結合！ -->
    WHERE o.user_id = #{userId}
    ORDER BY o.id DESC, oi.id ASC
  </select>

</mapper>